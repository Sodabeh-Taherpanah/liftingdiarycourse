# Args: $1 = target branch to merge into, $2 = new branch name to create

# Usage: /merge-and-create-branch <target-branch> <new-branch>

commit any changes in the current branch and provide a suitable commit message based on the code changes.Then merge the current branch into the $1 branch and resolve any issues of the back of that merge. then create a new branch called $2.
